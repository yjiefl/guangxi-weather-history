#!/bin/zsh

# 切换到脚本所在目录
cd "$(dirname "$0")"

echo "=========================================="
echo "    天气查询系统 - 彻底关闭服务"
echo "=========================================="

./stop.sh

echo ""
echo "按任意键退出..."
read -k 1 -s
